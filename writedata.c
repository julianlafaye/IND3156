#include <sensorda.h>
//writedata.c
void smalldata

int main () {

FILE* fp;
use(TempF);
use(TempC);
use(Hum);

	fp = fopen("bigdata.txt", "a");

	fprintf( &fp, "%d %d %d", TempC, TempF, Hum)

	smalldata(fp)
return(0)
}

void smalldata(fp) {
