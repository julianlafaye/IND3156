#include <sensorda.h>
//writedata.c
void smalldata

int main () {

FILE* fp;
use(TempF);
use(TempC);
use(Hum);

	fp = fopen("bigdata.txt");

	fprintf( &fp

	smalldata(fp)
return(0)
}

void smalldata(fp) {
