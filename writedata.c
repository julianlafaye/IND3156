#include <sensorda.h>
//writedata.c
void smalldata

int main () {

FILE* fp;
use(TempF);
use(TempC);
use(TempC);
use(Hum);
use(Hum2);
 for (int i = 1; i <= 1; i++)
    {
	fp = fopen("bigdata.txt", "a+");

		fprintf( &fp, "\n %d %d %d", TempC, TempF, Hum)

		fflush(fp)
		
		smalldata(fp, i)
	
	fclose(fp)
	}
return(0)
}

void smalldata(fp, i) {

tail(fp, i)


return	
}
